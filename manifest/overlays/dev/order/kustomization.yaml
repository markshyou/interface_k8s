apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../../base
images:
- name: ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/order-app
  newName: ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/order-app
  newTag: order-$(IMAGE_TAG)
patchesStrategicMerge:
- order-deployment-patch.yaml
- order-service-patch.yaml